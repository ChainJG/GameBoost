<UserControl
    x:Class="GameBoost.MVVM.UserControls.Shared.Display.IconDisplayInfoUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid>
        <Border
            Width="140"
            Height="140"
            CornerRadius="30"
            Style="{StaticResource GameBoostButtonCardBorderStyle}">

            <materialDesign:PackIcon
                Width="90"
                Height="90"
                Kind="{Binding Icon, RelativeSource={RelativeSource AncestorType=UserControl}, FallbackValue='QuestionMark'}"
                Style="{StaticResource WatermarkStyle}" />
        </Border>

        <StackPanel VerticalAlignment="Center">

            <TextBlock Style="{StaticResource TitleStyle}" Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}, FallbackValue='Title'}" />

            <TextBlock
                HorizontalAlignment="Center"
                Style="{StaticResource SubTitleStyle}"
                Text="{Binding SubTitle, RelativeSource={RelativeSource AncestorType=UserControl}, FallbackValue='SubTitle'}"
                TextAlignment="Center" />

        </StackPanel>
    </Grid>

</UserControl>