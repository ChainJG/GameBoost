<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <!--  🔥 Item Container Style  -->
    <Style x:Key="DockMenuListBoxItemStyle" TargetType="ListBoxItem">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Margin" Value="5,0,0,0" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border
                        x:Name="Root"
                        Width="60"
                        Height="60"
                        Background="Transparent"
                        CornerRadius="5">

                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>

                    <ControlTemplate.Triggers>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource ButtonMouseOver}" />
                            <Setter Property="Cursor" Value="Hand" />
                        </Trigger>

                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource ButtonIsChecked}" />
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  🔥 Item Template  -->
    <DataTemplate x:Key="DockMenuItemTemplate">
        <StackPanel HorizontalAlignment="Center" Orientation="Vertical">

            <materialDesign:PackIcon
                x:Name="PackIcon"
                Width="25"
                Height="25"
                Kind="{Binding Icon}" />

            <TextBlock
                x:Name="TitleText"
                Margin="0,2.5,0,0"
                Style="{StaticResource SubTitleStyle}"
                Text="{Binding Title}"
                Visibility="Visible" />

        </StackPanel>

        <DataTemplate.Triggers>


            <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                <Setter TargetName="TitleText" Property="Visibility" Value="Collapsed" />
                <DataTrigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetName="PackIcon"
                                Storyboard.TargetProperty="Width"
                                To="35"
                                Duration="0:0:0.1" />

                            <DoubleAnimation
                                Storyboard.TargetName="PackIcon"
                                Storyboard.TargetProperty="Height"
                                To="35"
                                Duration="0:0:0.1" />
                        </Storyboard>
                    </BeginStoryboard>
                </DataTrigger.EnterActions>

            </DataTrigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType={x:Type ListBoxItem}}, Path=IsMouseOver}" Value="True" />
                    <Condition Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="False" />
                </MultiDataTrigger.Conditions>

                <Setter TargetName="TitleText" Property="Visibility" Value="Collapsed" />

                <MultiDataTrigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetName="PackIcon"
                                Storyboard.TargetProperty="Width"
                                To="35"
                                Duration="0:0:0.1" />

                            <DoubleAnimation
                                Storyboard.TargetName="PackIcon"
                                Storyboard.TargetProperty="Height"
                                To="35"
                                Duration="0:0:0.1" />
                        </Storyboard>
                    </BeginStoryboard>
                </MultiDataTrigger.EnterActions>

                <MultiDataTrigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetName="PackIcon"
                                Storyboard.TargetProperty="Width"
                                To="25"
                                Duration="0:0:0.1" />

                            <DoubleAnimation
                                Storyboard.TargetName="PackIcon"
                                Storyboard.TargetProperty="Height"
                                To="25"
                                Duration="0:0:0.1" />
                        </Storyboard>
                    </BeginStoryboard>
                </MultiDataTrigger.ExitActions>
            </MultiDataTrigger>
        </DataTemplate.Triggers>

    </DataTemplate>

    <!--  🔥 Full ListBox Style  -->
    <Style x:Key="DockMenuListBoxStyle" TargetType="ListBox">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />

        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="ItemContainerStyle" Value="{StaticResource DockMenuListBoxItemStyle}" />

        <Setter Property="ItemTemplate" Value="{StaticResource DockMenuItemTemplate}" />
    </Style>

</ResourceDictionary>