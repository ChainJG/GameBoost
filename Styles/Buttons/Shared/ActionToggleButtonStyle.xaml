<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="clr-namespace:GameBoost.MVVM.AttachedProperties">

    <Style x:Key="SelectableActionToggleButtonStyle" TargetType="{x:Type ToggleButton}">

        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />

        <Setter Property="Template">
            <Setter.Value>

                <ControlTemplate TargetType="{x:Type ToggleButton}">

                    <Border
                        x:Name="CardBorder"
                        Padding="0"
                        ui:InteractiveCardAssist.HoverBorderBrush="Transparent"
                        ui:InteractiveCardAssist.IsSelected="{Binding IsChecked, RelativeSource={RelativeSource TemplatedParent}}"
                        ui:InteractiveCardAssist.NormalBackground="Transparent"
                        ui:InteractiveCardAssist.NormalBorderBrush="Transparent"
                        ui:InteractiveCardAssist.SelectedBorderBrush="Transparent"
                        BorderThickness=".7"
                        Style="{StaticResource GameBoostInteractiveButtonCardBorderStyle}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </Border>

                </ControlTemplate>
            </Setter.Value>
        </Setter>

    </Style>

</ResourceDictionary>