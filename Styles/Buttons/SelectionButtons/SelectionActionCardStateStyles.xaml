<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--  Used by action cards where the whole card is a ToggleButton.  -->
    <Style
        x:Key="SelectableActionToggleButtonStyle"
        BasedOn="{StaticResource ActionCardToggleButtonStyle}"
        TargetType="{x:Type ToggleButton}">

        <Setter Property="Opacity" Value="0.55" />

        <Style.Triggers>

            <Trigger Property="IsChecked" Value="True">
                <Setter Property="Opacity" Value="1" />
            </Trigger>

            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.35" />
            </Trigger>

        </Style.Triggers>
    </Style>


    <Style
        x:Key="SelectableActionBorderStyle"
        BasedOn="{StaticResource HoverableCardStyle}"
        TargetType="{x:Type Border}">

        <Setter Property="Opacity" Value="0.55" />

        <Style.Triggers>
            <DataTrigger Binding="{Binding IsChecked}" Value="True">
                <Setter Property="Opacity" Value="1" />
            </DataTrigger>

        </Style.Triggers>
    </Style>

</ResourceDictionary>