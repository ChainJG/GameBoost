<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="BaseTextStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{StaticResource PlainSubtitleTextColor}" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="FontFamily" Value="Bahnschrift" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FontWeight" Value="Light" />
    </Style>

    <Style
        x:Key="{x:Type TextBlock}"
        BasedOn="{StaticResource BaseTextStyle}"
        TargetType="{x:Type TextBlock}" />

    <Style
        x:Key="TitleStyle"
        BasedOn="{StaticResource BaseTextStyle}"
        TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource PlainTitleTextColor}" />
        <Setter Property="FontSize" Value="22" />
        <Setter Property="FontFamily" Value="Bahnschrift" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style
        x:Key="SubTitleStyle"
        BasedOn="{StaticResource BaseTextStyle}"
        TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource PlainSubtitleTextColor}" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="FontFamily" Value="Bahnschrift" />
        <Setter Property="FontWeight" Value="Light" />
    </Style>

    <Style
        x:Key="ButtonTitleStyle"
        BasedOn="{StaticResource BaseTextStyle}"
        TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource PlainTitleTextColor}" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontFamily" Value="Bahnschrift" />
        <Setter Property="FontWeight" Value="ExtraLight" />
    </Style>

    <Style
        x:Key="ButtonSubTitleStyle"
        BasedOn="{StaticResource BaseTextStyle}"
        TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource PlainSubtitleTextColor}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontFamily" Value="Bahnschrift" />
        <Setter Property="FontWeight" Value="UltraLight" />
    </Style>

</ResourceDictionary>